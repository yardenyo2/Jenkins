# Server Configuration
PORT=5001
NODE_ENV=test

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=root
DB_NAME=fullstack_boilerplate_test

# JWT Configuration
JWT_SECRET=test-jwt-secret
JWT_EXPIRES_IN=1h

# CORS Configuration
CORS_ORIGIN=http://localhost:3000 